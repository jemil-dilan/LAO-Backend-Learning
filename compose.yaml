services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=moneyTransfer'
      - 'POSTGRES_PASSWORD=ubuntu123'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
